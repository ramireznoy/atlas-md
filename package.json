{
  "name": "atlas-md",
  "version": "0.1.0",
  "description": "Local markdown browser and editor — browse and edit .md files via a zero-dependency Node.js server",
  "keywords": ["markdown", "browser", "editor", "viewer", "cli", "local"],
  "license": "MIT",
  "type": "module",
  "bin": { "atlas-md": "./server.js" },
  "files": [
    "server.js",
    "src/",
    "public/"
  ],
  "engines": { "node": ">=18.0.0" },
  "scripts": {
    "start": "node server.js",
    "test": "node --test test/auth.test.js test/fs.test.js test/router.test.js"
  }
}
